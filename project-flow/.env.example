# Prisma / PostgreSQL 连接 URL 示例
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/project_flow?schema=public"

# PostgreSQL 独立连接配置示例
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=project_flow

# 后端服务端口 (默认 3001)
PORT=3001
