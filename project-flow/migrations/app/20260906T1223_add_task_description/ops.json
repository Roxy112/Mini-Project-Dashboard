[
  {
    "id": "column.public.tasks.description",
    "label": "Add column \"description\" to \"tasks\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "description",
        "table": "tasks"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"description\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "tasks",
          "description"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"description\"",
        "sql": "ALTER TABLE \"public\".\"tasks\" ADD COLUMN \"description\" text"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"description\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "tasks",
          "description"
        ]
      }
    ]
  }
]